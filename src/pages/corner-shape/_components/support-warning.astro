<aside class="support-warning">
  <strong
    >Your browser doesn't support <code class="code">corner-shape</code> yet.</strong
  >{" "}
  So the enhanced preview below is generated with <code class="code"
    >clip-path</code
  >, just to show you the shape you're designing. Switch to Fallback and you'll
  see what this browser really gets from the generated CSS.
</aside>

<style>
  .support-warning {
    display: none;
    font-size: 1.1rem;

    margin-block: 0 3rem;
    padding-block: 0.6rem 0.51rem;
    padding-inline: 1.8rem;
    border-left: 3px solid var(--color-warning-border);
    border-right: 3px solid var(--color-warning-border);

    color: var(--color-warning-text);

    background: var(--color-info-surface);
  }

  :global(:root[data-corner-shape="unsupported"]) .support-warning {
    display: block;
  }
</style>
