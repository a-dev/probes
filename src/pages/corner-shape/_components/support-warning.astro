<aside class="support-warning">
  <strong>Your browser doesn't do <code class="code">corner-shape</code> yet.</strong
  >
  So the enhanced preview below is generated with <code class="code"
    >clip-path</code
  >, just to show you the shape you're designing. Switch to Fallback and you'll see
  what this browser really gets from the generated CSS.
</aside>

<style>
  .support-warning {
    display: none;

    margin-bottom: 1rem;
    padding: 0.75rem 1rem;
    border-left: 2px solid var(--color-warning-border);

    color: var(--color-warning-text);

    background: var(--color-transparent);
  }

  :global(:root[data-corner-shape="unsupported"]) .support-warning {
    display: block;
  }
</style>
